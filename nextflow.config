// enable DSL2
nextflow.enable.dsl=2

process {
  executor = 'local'
  maxForks = 10    // default to 10 parallel forks
}

profiles {
  aws {
    process {
      executor = 'awsbatch'
      // maybe bump forks up for AWS
      maxForks = 50
    }
  }
}
